module.exports = function(name, callback) {
    console.log('looking for universe ', name);
    if (!universes.hasOwnProperty(name)) {
        universes[name] = require(name);
    }
    callback(universes[name])
}

var universes = {};